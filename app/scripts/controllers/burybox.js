/* global angular */
(function() {
  'use strict';

angular.module('burybox', ['ngRoute', 'restangular', 'firebase'])
  .controller('MainController', function(){

  return('hello world');

});


})();
